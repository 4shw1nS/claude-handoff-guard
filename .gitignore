# Local user config (created from config.example.sh at install time)
config.sh
# Runtime state / logs
*.log
state/
# Handoffs this tool writes into whatever project it runs in — including this repo
HANDOFF.md
HANDOFF.md.bak
